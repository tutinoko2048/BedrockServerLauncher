{
  "name": "@bds-utils/updater-executable",
  "private": true,
  "scripts": {
    "build:windows": "bun build src/main.ts --minify --compile --target=bun-windows-x64 --outfile build/bds-updater",
    "build:linux": "bun build src/main.ts --minify --compile --target=bun-linux-x64 --outfile build/bds-updater"
  },
  "dependencies": {
    "@bds-utils/updater-core": "workspace:*",
    "picocolors": "^1.1.1"
  }
}